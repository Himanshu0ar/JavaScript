const marvel_heroes = ["Thor", "IronMan", "SpiderMan"]
const dc_heroes = ["SuperMan", "Flash", "BatMan"]

//marvel_heroes.push(dc_heroes)     //it will return dc_heroes array in marvel_heroes array
//console.log(marvel_heroes)

// const allHeroes = marvel_heroes.concat(dc_heroes)       //return a new array which is the combination of elements of two arrays.
// console.log(allHeroes)

// const allNewHeroes = [...dc_heroes ,...marvel_heroes]     //it is known as spread method and it is mostly used. it an accept two or more than two array in it.
// console.log(allNewHeroes)

// const another_array = [1,2,3,[4,5,6],7,[6,7,[4,5]]]
// const flat_newArray = another_array.flat(Infinity)     //flat all arrays inside in ine array.
// console.log(flat_newArray)


// console.log(Array.isArray("Himanshu"))   //used to ask given material is array or not.
// console.log(Array.from("Himanshu"))      //convert give material into array.  o/p: ["h","i","m"......]

//console.log(Array.from({name: "Himanshu"}))   //it will return empty array as it is unable to convert key-value pair into array.we have to tell more to conver key or convert values into array.


no1 = 1
no2 = 2
no3 = 3
//console.log(Array.of(no1,no2,no3))


array.forEach(element => {
    
});